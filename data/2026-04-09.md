<div id=toc></div>

# Table of Contents

- [cs.AR](#cs.AR) [Total: 5]
- [cs.ET](#cs.ET) [Total: 2]
- [cs.DC](#cs.DC) [Total: 13]


<div id='cs.AR'></div>

# cs.AR [[Back]](#toc)

### [1] [CoverAssert: Iterative LLM Assertion Generation Driven by Functional Coverage via Syntax-Semantic Representations](https://arxiv.org/abs/2604.06607)
*Yonghao Wang,Yang Yin,Hongqin Lyu,Jiaxin Zhou,Zhiteng Chao,Mingyu Shi,Wenchao Ding,Yunlin Du,Jing Ye,Tiancheng Wang,Huawei Li*

Main category: cs.AR

TL;DR: CoverAssert is an iterative framework that improves LLM-generated SystemVerilog assertions by clustering assertion features, mapping them to specifications, and using functional coverage feedback to guide LLMs toward uncovered points.


<details>
  <summary>Details</summary>
Motivation: Current LLMs generate SystemVerilog assertions from natural language specs but produce single-pass outputs with limited functional coverage due to insufficient IC design understanding.

Method: CoverAssert clusters semantic and AST-based structural features of assertions, maps them to specifications, and uses functional coverage feedback to iteratively guide LLMs in prioritizing uncovered points.

Result: Experiments on four open-source designs show CoverAssert integrated with AssertLLM and Spec2Assertion improves average coverage by 9.57% in branch coverage, 9.64% in statement coverage, and 15.69% in toggle coverage.

Conclusion: CoverAssert effectively enhances LLM-generated SystemVerilog assertions by incorporating iterative feedback and coverage analysis, significantly improving functional coverage metrics.

Abstract: LLMs can generate SystemVerilog assertions (SVAs) from natural language specs, but single-pass outputs often lack functional coverage due to limited IC design understanding. We propose CoverAssert, an iterative framework that clusters semantic and AST-based structural features of assertions, maps them to specifications, and uses functional coverage feedback to guide LLMs in prioritizing uncovered points. Experiments on four open-source designs show that integrating CoverAssert with AssertLLM and Spec2Assertion improves average improvements of 9.57 % in branch coverage, 9.64 % in statement coverage, and 15.69 % in toggle coverage.

</details>


### [2] [SwarmIO: Towards 100 Million IOPS SSD Emulation for Next-generation GPU-centric Storage Systems](https://arxiv.org/abs/2604.06668)
*Hyeseong Kim,Gwangoo Yeo,Minsoo Rhu*

Main category: cs.AR

TL;DR: SwarmIO is an SSD emulator designed for GPU-centric storage systems that achieves up to 40 MIOPS performance and 303.9x speedup over existing emulators for GPU-initiated I/O workloads.


<details>
  <summary>Details</summary>
Motivation: The emergence of GPU-initiated I/O and IOPS-optimized SSDs enables GPU-centric storage systems, but existing SSD emulators fail to handle massive GPU thread-level parallelism and high request rates needed for quantitative evaluation.

Method: SwarmIO addresses three key challenges: limited frontend scalability for massive request streams, high software overhead in emulating GPU-initiated I/O paths, and excessive timing-model maintenance at high I/O rates.

Result: SwarmIO achieves 303.9x speedup over state-of-the-art SSD emulators and faithfully models IOPS-optimized SSDs up to 40 MIOPS. A vector search case study shows 9.7x end-to-end speedup when increasing SSD IOPS from 2.5 to 40 MIOPS.

Conclusion: SwarmIO enables quantitative end-to-end evaluation of GPU-centric storage systems by overcoming limitations of conventional SSD emulators, providing a crucial tool for developing IOPS-optimized, GPU-initiated I/O storage architectures.

Abstract: GPU-initiated I/O has emerged as a key mechanism for achieving high-throughput storage access by leveraging massive GPU thread-level parallelism, while recent industry trends point toward SSDs optimized for ultra-high random-read IOPS. Together, these trends are enabling the emergence of IOPS-optimized, GPU-centric storage systems. Despite this momentum, no existing framework enables quantitative end-to-end evaluation of storage systems optimized for GPU-initiated I/O. While conventional SSD emulators provide a promising path toward end-to-end modeling in traditional storage systems, they face three key challenges in this GPU-centric setting: limited frontend scalability for ingesting massive request streams, high software overhead in emulating GPU-initiated I/O control and data paths, and excessive timing-model maintenance overhead at extremely high I/O request rates. We propose SwarmIO, an SSD emulator for massively parallel, GPU-centric storage. SwarmIO faithfully models IOPS-optimized SSDs at target performance levels of up to 40 MIOPS, achieving a 303.9x speedup over the state-of-the-art baseline SSD emulator under GPU-initiated I/O. We further demonstrate its utility through a vector search case study, showing that increasing SSD IOPS from 2.5 MIOPS to 40 MIOPS yields an average end-to-end speedup of up to 9.7x.

</details>


### [3] [CBM-Dual: A 65-nm Fully Connected Chaotic Boltzmann Machine Processor for Dual Function Simulated Annealing and Reservoir Computing](https://arxiv.org/abs/2604.06808)
*Kanta Yoshioka,Soshi Hirayae,Yuichiro Tanaka,Yuichi Katori,Takashi Morie,Hakaru Tamukoh*

Main category: cs.AR

TL;DR: CBM-Dual is the first silicon-proven digital chaotic dynamics processor supporting both simulated annealing and reservoir computing, achieving significant efficiency improvements for Edge AI applications.


<details>
  <summary>Details</summary>
Motivation: To enable real-time decision-making and lightweight adaptation for autonomous Edge AI by addressing the high computational and area costs of digital chaotic dynamics processors.

Method: 1) CBM-specific scheduler exploiting low neuron flip rate to reduce multiply-accumulate operations by 99%, 2) efficient multiply splitting scheme reducing area by 59%, 3) implementation of largest-scale fully connected 1024-neuron chaotic Boltzmann machine.

Result: Fabricated in 65nm (12mm²), CBM-Dual achieves simultaneous heterogeneous task execution and state-of-the-art energy efficiency with ×25-54 improvement in SA and ×4.5 improvement in RC fields.

Conclusion: CBM-Dual demonstrates the first silicon-proven digital CDP supporting both SA and RC, providing significant efficiency gains for Edge AI applications through innovative scheduling and architectural optimizations.

Abstract: This paper presents CBM-Dual, the first silicon-proven digital chaotic dynamics processor (CDP) supporting both simulated annealing (SA) and reservoir computing (RC). CBM-Dual enables real-time decision-making and lightweight adaptation for autonomous Edge AI, employing the largest-scale fully connected 1024-neuron chaotic Boltzmann machine (CBM). To address the high computational and area costs of digital CDPs, we propose: 1) a CBM-specific scheduler that exploits an inherently low neuron flip rate to reduce multiply-accumulate operations by 99%, and 2) an efficient multiply splitting scheme that reduces the area by 59%. Fabricated in 65nm (12mm$^2$), CBM-Dual achieves simultaneous heterogeneous task execution and state-of-the-art energy efficiency, delivering $\times$25-54 and $\times$4.5 improvements in the SA and RC fields, respectively.

</details>


### [4] [TRAPTI: Time-Resolved Analysis for SRAM Banking and Power Gating Optimization in Embedded Transformer Inference](https://arxiv.org/abs/2604.06955)
*Jan Klhufek,Alberto Marchisio,Vojtech Mrazek,Lukas Sekanina,Muhammad Shafique*

Main category: cs.AR

TL;DR: TRAPTI is a two-stage methodology combining cycle-level inference simulation with time-resolved memory analysis to optimize KV cache management and memory organization for transformer deployment on embedded hardware.


<details>
  <summary>Details</summary>
Motivation: Transformer deployment on embedded hardware faces challenges from KV cache memory footprint growth with sequence length, straining on-chip memory. Existing mechanisms like GQA reduce requirements but need understanding of temporal memory demand evolution.

Method: Two-stage methodology: 1) Obtain memory occupancy traces and access statistics from cycle-level inference simulation; 2) Use traces to explore banked memory organizations and power-gating configurations in offline optimization flow.

Result: Applied to GPT-2 XL and DeepSeek-R1-Distill-Qwen-1.5B under same accelerator configuration. DeepSeek shows 2.72x reduction in peak on-chip memory utilization compared to GPT-2 XL, enabling better power-gating optimization opportunities.

Conclusion: TRAPTI enables direct comparison of MHA and GQA memory profiles, revealing significant memory efficiency improvements with GQA and providing methodology for optimizing memory organization and power management for transformer deployment.

Abstract: Transformer neural networks achieve state-of-the-art accuracy across language and vision tasks, but their deployment on embedded hardware is hindered by stringent area, latency, and energy constraints. During inference, performance and efficiency are increasingly dominated by the Key--Value (KV) cache, whose memory footprint grows with sequence length, straining on-chip memory utilization. Although existing mechanisms such as Grouped-Query Attention (GQA) reduce KV cache requirements compared to Multi-Head Attention (MHA), effectively exploiting this reduction requires understanding how on-chip memory demand evolves over time. This work presents TRAPTI, a two-stage methodology that combines cycle-level inference simulation with time-resolved analysis of on-chip memory occupancy to guide design decisions. In the first stage, the framework obtains memory occupancy traces and memory access statistics from simulation. In the second stage, the framework leverages the traces to explore banked memory organizations and power-gating configurations in an offline optimization flow. We apply this methodology to GPT-2 XL and DeepSeek-R1-Distill-Qwen-1.5B under the same accelerator configuration, enabling a direct comparison of MHA and GQA memory profiles. The analysis shows that DeepSeek-R1-Distill-Qwen-1.5B exhibits a 2.72x reduction in peak on-chip memory utilization in this setting compared to GPT-2 XL, unlocking further opportunities for power-gating optimization.

</details>


### [5] [Symbolic Polyhedral-Based Energy Analysis for Nested Loop Programs](https://arxiv.org/abs/2604.07287)
*Avinash Mahesh Nirmala,Dominik Walter,Frank Hannig,Jürgen Teich*

Main category: cs.AR

TL;DR: Symbolic energy analysis method for nested loop programs on parallel processor arrays, enabling design space exploration without simulation.


<details>
  <summary>Details</summary>
Motivation: To overcome limitations of simulation-based energy evaluation which is not scalable and computationally expensive for exploring mapping and scheduling decisions on parallel processor array accelerators.

Method: Developed a symbolic approach for energy consumption estimation that captures the impact of mapping and scheduling decisions on processor arrays, deriving analytical formulas instead of relying on simulation.

Result: The symbolic analysis is independent of problem size (unlike simulations), validated against simulation-based results for selected benchmarks with varying iteration space sizes.

Conclusion: The symbolic energy analysis methodology enables efficient design space exploration for mapping/scheduling decisions, study of array size variations, and comparison with other accelerator architectures.

Abstract: This work presents a symbolic approach for estimating the energy consumption for nested loop programs when mapped and scheduled on parallel processor array accelerator architectures. Instead of simulation-based evaluation, we derive a methodology for symbolic energy analysis that captures the impact of mapping and scheduling decisions of loop nests on processor arrays. We compare our approach against simulation-based results for selected benchmarks and varying sizes of the iteration spaces. Whereas the latter are not scalable, our symbolic analysis is shown to be independent of the problem size. The presented evaluation methodology can be beneficially used during the design space exploration of mapping and scheduling decisions, for studying the influence of array size variations, and for comparisons with other loop nest accelerator architectures.

</details>


<div id='cs.ET'></div>

# cs.ET [[Back]](#toc)

### [6] [Computing In Spintronic Memory: A Thermal Perspective](https://arxiv.org/abs/2604.06667)
*Patrick Miller,Hüsrev Cilasun,Sachin S. Sapatnekar,Ulya R. Karpuzcu*

Main category: cs.ET

TL;DR: This paper provides a quantitative thermal characterization of Computing-in-Memory (CiM) architectures, analyzing how temperature varies with computational activity, array size, and memory technology.


<details>
  <summary>Details</summary>
Motivation: While CiM is promising for addressing memory bottlenecks and enabling efficient Boolean operations in non-volatile memory arrays, the increased microarchitectural activity can significantly raise power density and create thermal hotspots, which need to be quantitatively characterized.

Method: The paper conducts thermal characterization of CiM architectures, analyzing temperature distribution and variations based on computational parameters including number of active memory cells, array size, and memory technology.

Result: Four key findings: (1) temperature remains mostly uniform due to lateral thermal conduction; (2) temperature increases linearly with number of memory cells participating in computation; (3) temperature decreases linearly with memory array size; (4) memory technology dictates power density and thermal characteristics.

Conclusion: The thermal behavior of CiM architectures follows predictable linear relationships with computational activity and array size, with memory technology being a key determinant of thermal characteristics, providing important insights for thermal-aware CiM design.

Abstract: Computing-in-Memory (CiM) is a promising paradigm to address the memory bottleneck constraining traditional systems. Most power-efficient CiM variants can directly perform Boolean operations in non-volatile memory arrays. Higher microarchitectural activity due to CiM, however, can significantly increase power density (power per area) and result in thermal hotspots. In this paper, we provide a quantitative thermal characterization for CiM. We demonstrate that (i) the temperature remains mostly uniform due to lateral thermal conduction; (ii) the temperature increases linearly with the number of memory cells participating in computation; (iii) the temperature decreases linearly with the memory array size; (iv) the memory technology dictates the power density, hence the thermal characteristics.

</details>


### [7] [Improving Feasibility in Quantum Approximate Optimization Algorithm for Vehicle Routing via Constraint-Aware Initialization and Hybrid XY-X Mixing](https://arxiv.org/abs/2604.07218)
*Yuan-Zheng Lei,Yaobang Gong,Xianfeng Terry Yang,Nii Attoh-Okine*

Main category: cs.ET

TL;DR: Constraint-aware QAOA framework for Vehicle Routing Problem with specialized initialization and hybrid mixer improves solution quality and feasibility over standard QAOA across simulation regimes.


<details>
  <summary>Details</summary>
Motivation: Standard QAOA struggles with VRP because feasible solutions are extremely sparse in search space, and conventional Pauli-X mixer disrupts partial solutions satisfying local constraints.

Method: Two-component framework: 1) Lightweight initialization encoding local one-hot constraints into initial state, 2) Hybrid XY-X mixer preserving constraint structure while allowing exploration of unconstrained degrees of freedom.

Result: Proposed method consistently achieves lower average energy and higher feasible-solution ratios than standard QAOA across ideal simulation, finite-shot sampling, and noisy regimes, though gap narrows under noise.

Conclusion: Constraint-aware QAOA effectively guides toward valid VRP solutions; practical advantage likely to grow as quantum hardware improves and error rates decline.

Abstract: The Quantum Approximate Optimization Algorithm (QAOA) is a leading framework for quantum combinatorial optimization. The Vehicle Routing Problem (VRP), a core problem in logistics and transportation, is a natural application target, but it poses a major feasibility challenge for standard QAOA because feasible solutions occupy only a tiny fraction of the search space, and the conventional Pauli-$X$ mixer can disrupt partial solution structures that satisfy key local constraints. To address this issue, we propose a constraint-aware QAOA framework with two complementary components. First, we design a lightweight initialization strategy that encodes a selected subset of simple yet informative local one-hot constraints into the initial state, thereby reducing the initial superposition space and increasing the probability mass on states with important local structure. Second, we introduce a hybrid XY-$X$ mixer that preserves the constraint structure imposed at initialization while retaining exploratory flexibility over the remaining unconstrained degrees of freedom during QAOA evolution. We evaluate the proposed framework against standard QAOA under three progressively more realistic regimes: ideal statevector simulation, finite-shot sampling, and noisy finite-shot sampling. Across all regimes, the proposed method consistently achieves lower average energy and higher feasible-solution ratios than standard QAOA, indicating more effective guidance toward structurally valid, lower-cost VRP solutions. However, the performance gap narrows in the noisy regime. Because this setting adopts a hardware-inspired error model based on near-best-reported laboratory-level qubit gate and readout fidelities, the observed attenuation suggests that the practical advantage of the more structured mixer is likely to grow as quantum hardware improves and error rates decline.

</details>


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [8] [Scheduling the Unschedulable: Taming Black-Box LLM Inference at Scale](https://arxiv.org/abs/2604.06970)
*Renzhong Yuan,Yijun Zeng,Xiaosong Gao,Linxi Yu,Haochun Liao,Han Wang*

Main category: cs.DC

TL;DR: Client-side scheduling for black-box LLM APIs using token count predictions achieves near-isolation performance with 100% completion and deadline satisfaction under balanced/high congestion.


<details>
  <summary>Details</summary>
Motivation: When output token counts can be predicted at submission time, client-side scheduling against black-box LLM APIs becomes semi-clairvoyant, enabling better resource allocation decisions despite provider internals remaining hidden.

Method: Three-layer decomposition: 1) Allocation (inter-class share via adaptive DRR), 2) Ordering (intra-class sequencing with feasible-set scoring), and 3) Overload control (explicit admit/defer/reject on a cost ladder).

Result: Under balanced/high congestion: 100% completion, 100% deadline satisfaction, useful goodput of 4.2 ± 1.6 SLO-meeting requests/s, short P95 within tens of milliseconds of quota-tiered isolation. Predictor noise up to 60% multiplicative error tolerated gracefully.

Conclusion: Coarse magnitude priors (not just class labels) are crucial for effective client control; the three-layer decomposition accommodates different fairness objectives; cost-ladder shedding effectively links to service objectives; allocation layer enables policy flexibility without changing the rest of the stack.

Abstract: When output token counts can be predicted at submission time (Gan et al., 2026), client-side scheduling against a black-box LLM API becomes semi-clairvoyant: decisions condition on coarse token priors even though the provider's internals remain hidden. We decompose this boundary problem into three separable concerns: allocation (inter-class share via adaptive DRR), ordering (intra-class sequencing with feasible-set scoring), and overload control (explicit admit/defer/reject on a cost ladder). An information ladder experiment shows that coarse magnitude priors -- not class labels alone -- are the practical threshold for useful client control; removing magnitude inflates short-request P95 by up to $5.8\times$ and degrades deadline satisfaction. Under balanced / high congestion the full stack achieves 100% completion, 100% deadline satisfaction, and useful goodput of $4.2 \pm 1.6$ SLO-meeting requests/s with short P95 within tens of milliseconds of quota-tiered isolation. A predictor-noise sweep confirms graceful degradation under up to 60% multiplicative error. Heavy-dominated regimes separate policies on completion, tail, and interpretable shedding. We further compare short-priority allocation (biased toward interactive traffic) with Fair Queuing (round-robin across classes): Fair Queuing achieves +32% short-request P90 improvement over FIFO with only +17% long-request overhead, versus Short-Priority's +27% / +116% trade-off -- demonstrating that the allocation layer accommodates different fairness objectives without changing the remaining stack. We contribute the three-layer client-side decomposition, controlled evaluation of joint metrics across regimes, allocation-policy alternatives, and overload-policy evidence linking cost-ladder shedding to the stated service objective.

</details>


### [9] [ForkKV: Scaling Multi-LoRA Agent Serving via Copy-on-Write Disaggregated KV Cache](https://arxiv.org/abs/2604.06370)
*Shao Wang,Rui Ren,Lin Gui*

Main category: cs.DC

TL;DR: ForkKV is a serving system for multi-agent LLM workflows that uses OS-like fork with copy-on-write to efficiently manage KV cache memory when hosting specialized LoRA agents, achieving 3x throughput improvement.


<details>
  <summary>Details</summary>
Motivation: Multi-agent LLM workflows with specialized LoRA agents create memory bottlenecks because unique LoRA activations cause KV cache divergence, making traditional prefix caching ineffective and forcing redundant cache maintenance that saturates GPU capacity.

Method: ForkKV introduces a fork with copy-on-write memory management paradigm, physically decoupling KV cache into shared and agent-specific components. It uses DualRadixTree architecture for cache inheritance and ResidualAttention kernel for efficient on-chip SRAM reconstruction.

Result: Comprehensive evaluations show ForkKV achieves up to 3.0x throughput of state-of-the-art multi-LoRA serving systems with negligible impact on generation quality across diverse language models and practical datasets.

Conclusion: ForkKV effectively addresses the memory bottleneck in multi-LoRA agent serving by applying OS memory management principles to KV cache, enabling efficient co-hosting of specialized agents while maintaining high throughput and generation quality.

Abstract: The serving paradigm of large language models (LLMs) is rapidly shifting towards complex multi-agent workflows where specialized agents collaborate over massive shared contexts. While Low-Rank Adaptation (LoRA) enables the efficient co-hosting of these specialized agents on a single base model, it introduces a critical memory footprint bottleneck during serving. Specifically, unique LoRA activations cause Key-Value (KV) cache divergence across agents, rendering traditional prefix caching ineffective for shared contexts. This forces redundant KV cache maintenance, rapidly saturating GPU capacity and degrading throughput.
  To address this challenge, we introduce ForkKV, a serving system for multi-LoRA agent workflows centered around a novel memory management paradigm in OS: fork with copy-on-write (CoW). By exploiting the structural properties of LoRA, ForkKV physically decouples the KV cache into a massive shared component (analogous to the parent process's memory pages) and lightweight agent-specific components (the child process's pages). To support this mechanism, we propose a DualRadixTree architecture that allows newly forked agents to inherit the massive shared cache and apply CoW semantics for their lightweight unique cache. Furthermore, to guarantee efficient execution, we design ResidualAttention, a specialized kernel that reconstructs the disaggregated KV cache directly within on-chip SRAM. Comprehensive evaluations across diverse language models and practical datasets of different tasks demonstrate that ForkKV achieves up to 3.0x the throughput of state-of-the-art multi-LoRA serving systems with a negligible impact on generation quality.

</details>


### [10] [Contextual Chain: Single-State Ledger Design for Mobile/IoT Networks with Frequent Partitions](https://arxiv.org/abs/2604.06529)
*Song-Ju Kim*

Main category: cs.DC

TL;DR: A lightweight ledger protocol for IoT/mobile networks uses contextual authentication and adaptive synchronization to handle partitions and noise, with evaluation showing synchronization is more critical than quarantine for recovery.


<details>
  <summary>Details</summary>
Motivation: IoT and mobile networks face intermittent connectivity and partitions where full-history verification is impractical, requiring lightweight protocols that can operate in noisy, partitioned environments.

Method: Design uses contextual authentication with checkpoint-first fork choice, local branch scoring, and quarantine signals, combined with adaptive synchronization that increases gossip effort when inconsistency is detected.

Result: Quarantine alone doesn't improve agreement/recovery under noise, but increased synchronization (Gossip_only and Both) significantly improves final agreement probability and recovery-time tails after partition rejoin.

Conclusion: Under noisy partition/rejoin dynamics, recovery is limited primarily by information availability, making synchronization policy a first-class design problem rather than relying on quarantine mechanisms.

Abstract: We study a lightweight ledger protocol for intermittent and noisy networks, motivated by IoT and mobile settings in which partitions are common and full-history verification is impractical. Our design centers on an \emph{operational} notion of \textbf{contextual authentication}: each node decides whether a chain extension is acceptable in its current local context, using checkpoint-first fork choice, a local branch score derived from recent proposer behavior, and an inconsistency-driven \emph{quarantine} signal. To improve recovery after partitions, we combine this acceptance rule with \textbf{adaptive synchronization}, which increases gossip effort only when inconsistency becomes prevalent.
  We evaluate the protocol with a discrete-event simulator under controlled partitions and two network regimes (clean and noisy). Across 500 seeds at $N=20$, the main result is that quarantine alone does not materially improve agreement or recovery under noisy conditions, whereas increased synchronization (\texttt{Gossip\_only} and \texttt{Both}) substantially improves both final agreement probability and recovery-time tails after partition rejoin. Longer-horizon experiments show that low-synchronization failures are not removed simply by waiting longer, and scaling experiments at $N=50$ and $N=100$ show that parameters that work at small scale do not automatically generalize. These results indicate that, under noisy partition/rejoin dynamics, recovery in the current design is limited primarily by information availability, making synchronization policy a first-class design problem.

</details>


### [11] [DynLP: Parallel Dynamic Batch Update for Label Propagation in Semi-Supervised Learning](https://arxiv.org/abs/2604.06596)
*S M Shovan,Arindam Khanda,S M Ferdous,Sajal K. Das,Mahantesh Halappanavar*

Main category: cs.DC

TL;DR: DynLP: GPU-accelerated dynamic batched parallel label propagation for efficient incremental semi-supervised learning on graphs


<details>
  <summary>Details</summary>
Motivation: Traditional label propagation requires full recalculation when new data arrives incrementally, which is redundant and computationally expensive for real-world streaming applications.

Method: DynLP performs only necessary updates by propagating changes to relevant subgraphs without full recalculation, leveraging GPU architectural optimizations for parallel processing.

Result: Achieves average 13x and up to 102x speedup on large-scale datasets compared to state-of-the-art approaches.

Conclusion: DynLP provides an efficient solution for incremental graph-based semi-supervised learning with significant performance improvements through GPU-centric dynamic updates.

Abstract: Semi-supervised learning aims to infer class labels using only a small fraction of labeled data. In graph-based semi-supervised learning, this is typically achieved through label propagation to predict labels of unlabeled nodes. However, in real-world applications, data often arrive incrementally in batches. Each time a new batch appears, reapplying the traditional label propagation algorithm to recompute all labels is redundant, computationally intensive, and inefficient. To address the absence of an efficient label propagation update method, we propose DynLP, a novel GPU-centric Dynamic Batched Parallel Label Propagation algorithm that performs only the necessary updates, propagating changes to the relevant subgraph without requiring full recalculation. By exploiting GPU architectural optimizations, our algorithm achieves on average 13x and upto 102x speedup on large-scale datasets compared to state-of-the-art approaches.

</details>


### [12] [Sparsity-Aware Roofline Models for Sparse Matrix-Matrix Multiplication](https://arxiv.org/abs/2604.06637)
*Matthew Qian,Yahia Ramadan,Suhita Anubha,Ariful Azad*

Main category: cs.DC

TL;DR: Sparse matrix-dense matrix multiplication (SpMM) performance analysis shows that traditional roofline models fail to predict performance across diverse sparsity patterns, requiring sparsity-aware modeling that accounts for matrix structure, memory traffic, and blocking behavior.


<details>
  <summary>Details</summary>
Motivation: SpMM is a critical kernel in scientific computing, graph analytics, and machine learning, but its performance is often memory-bandwidth constrained. Traditional roofline models don't account for how different sparsity patterns affect arithmetic intensity and performance, leading to inaccurate predictions.

Method: Evaluated three SpMM implementations (CSR, CSB, Intel MKL) using large-scale matrices from SuiteSparse collection, grouped by sparsity pattern (block-structured, banded/diagonal, scale-free, uniformly random). Derived sparsity-aware roofline models incorporating memory traffic, cache locality, and blocking behavior. Conducted experiments on AMD-based Perlmutter compute node with varying dense matrix columns.

Result: Blocking and structured sparsity significantly alter effective arithmetic intensity. A single roofline model is insufficient to accurately predict performance across diverse matrix structures. Different sparsity patterns require different modeling approaches to account for their unique memory access patterns and computational characteristics.

Conclusion: Accurate roofline-based performance analysis of SpMM requires sparsity-aware modeling that accounts for matrix structure. Data layout and blocking strategies must be evaluated in context of specific sparsity patterns rather than through a single unified model.

Abstract: Sparse matrix-dense matrix multiplication (SpMM) is a critical kernel in scientific computing, graph analytics, and machine learning, whose performance is often constrained by memory bandwidth. In this work, we investigate the applicability and limitations of roofline modeling for SpMM by explicitly accounting for the impact of matrix sparsity structure on arithmetic intensity and attainable performance. We evaluate three SpMM implementations: Compressed Sparse Row (CSR), Compressed Sparse Blocks (CSB), and Intel's Math Kernel Library (MKL). Each implementation was tested using large-scale matrices from the SuiteSparse collection and grouped by sparsity pattern, including block-structured, banded (diagonal), scale-free, and uniformly random matrices. We derive sparsity-aware roofline models that incorporate memory traffic, cache locality, and blocking behavior, and demonstrate that a single model is insufficient to accurately predict performance across diverse structures. Experiments were conducted on an AMD-based Perlmutter compute node with a varying number of columns in the dense matrix. In particular, blocking and structured sparsity significantly alter effective arithmetic intensity. The results show that accurate roofline-based performance analysis of SpMM requires sparsity-aware modeling, and that data layout and blocking strategies must be evaluated in the context of matrix structure rather than through a single unified model.

</details>


### [13] [Foundry: Template-Based CUDA Graph Context Materialization for Fast LLM Serving Cold Start](https://arxiv.org/abs/2604.06664)
*Xueshen Liu,Yongji Wu,Yuncheng Yao,Danyang Zhuo,Ion Stoica,Z. Morley Mao*

Main category: cs.DC

TL;DR: Foundry reduces CUDA graph cold-start latency by 99% through template-based serialization, cutting Qwen3-235B-A22B initialization from 10 minutes to 3.9 seconds.


<details>
  <summary>Details</summary>
Motivation: CUDA graph capture dominates cold-start latency in LLM serving (tens of seconds to minutes), but existing serialization approaches are either brittle (kernel-specific patching) or heavyweight (process-level checkpoint/restore), and inflexible to dynamic parallelism switching.

Method: Template-based CUDA graph context materialization system that persists both graph topology and execution context offline, then reconstructs executable graphs online with negligible overhead. Enforces deterministic memory layouts, extracts/reloads kernel binaries, uses topology-based templating, and enables single-GPU capture to generate multi-GPU templates by patching rank-dependent communication state.

Result: Reduces cold-start latency by up to 99%, cutting Qwen3-235B-A22B initialization from 10 minutes to 3.9 seconds while preserving CUDA graph throughput gains. Works across dense and MoE models up to 235B parameters.

Conclusion: Foundry effectively solves the CUDA graph cold-start bottleneck through template-based serialization, enabling rapid autoscaling and parallelism reconfiguration for modern LLM serving while maintaining performance benefits.

Abstract: Modern LLM service providers increasingly rely on autoscaling and parallelism reconfiguration to respond to rapidly changing workloads, but cold-start latency remains a major bottleneck. While recent systems have reduced model weight loading to seconds, CUDA graph capture still takes tens of seconds to minutes and often dominates startup. Unfortunately, CUDA graphs cannot be naively serialized: beyond graph topology, they are tightly coupled to execution context, including device addresses embedded in kernel arguments and kernel code lazily loaded during warmup. Existing approaches either rely on brittle kernel-specific patching or heavyweight process-level checkpoint/restore that are inflexible to dynamic parallelism switching. We present Foundry, a template-based CUDA graph context materialization system that persists both graph topology and execution context during an offline processing stage, and reconstructs executable graphs online with negligible overhead. Foundry enforces deterministic memory layouts, automatically extracts and reloads kernel binaries required by captured graphs, and reduces online reconstruction costs through topology-based templating. For distributed serving, Foundry further enables a single-GPU offline capture to generate templates for multi-GPU deployments by patching only rank-dependent communication state. Across dense and MoE models up to 235B parameters, Foundry reduces cold-start latency by up to 99%, cutting the initialization time of Qwen3-235B-A22B from 10 minutes to 3.9 seconds while preserving the throughput gains of CUDA graphs.

</details>


### [14] [Beyond End-to-End: Dynamic Chain Optimization for Private LLM Adaptation on the Edge](https://arxiv.org/abs/2604.06819)
*Yebo Wu,Jingguang Li,Chunlin Tian,Kahou Tam,Zhijiang Guo,Li Li*

Main category: cs.DC

TL;DR: ChainFed enables federated fine-tuning of large language models on edge devices by training adapters layer-by-layer instead of end-to-end, overcoming memory limitations while preserving privacy.


<details>
  <summary>Details</summary>
Motivation: Federated fine-tuning for LLMs faces a critical bottleneck: the high memory demands of LLMs conflict with edge devices' limited capacity, creating a memory barrier that prevents effective privacy-preserving model adaptation.

Method: ChainFed uses sequential layer-by-layer training instead of end-to-end updates. It trains initial adapter to convergence, freezes weights, then proceeds to next layer. Includes three core techniques: Dynamic Layer Co-Tuning (bridges semantic gaps), Globally Perceptive Optimization (provides foresight beyond local objectives), and Function-Oriented Adaptive Tuning (identifies optimal fine-tuning starting point).

Result: Extensive experiments on multiple benchmarks demonstrate ChainFed's superiority over existing methods, boosting average accuracy by up to 46.46%.

Conclusion: ChainFed successfully breaks the memory barrier in federated LLM fine-tuning through its innovative layer-by-layer sequential optimization approach, enabling effective privacy-preserving adaptation on resource-constrained edge devices.

Abstract: Federated fine-tuning enables privacy-preserving LLM adaptation but faces a critical bottleneck: the disparity between LLMs' high memory demands and edge devices' limited capacity. To break the memory barrier, we propose Chain Federated Fine-Tuning (ChainFed), an innovative paradigm that forgoes end-to-end updates in favor of a sequential, layer-by-layer manner. It first trains the initial adapter to convergence, freezes its weights, and then proceeds to the next. This iterative train-and-freeze process forms an optimization chain, gradually enhancing the model's task-specific proficiency. ChainFed further integrates three core techniques: 1) Dynamic Layer Co-Tuning to bridge semantic gaps between sequentially tuned layers and facilitate information flow; 2) Globally Perceptive Optimization to endow each adapter with foresight beyond its local objective; 3) Function-Oriented Adaptive Tuning to automatically identify the optimal fine-tuning starting point. Extensive experiments on multiple benchmarks demonstrate the superiority of ChainFed over existing methods, boosting average accuracy by up to 46.46\%.

</details>


### [15] [Exploiting Aggregate Programming in a Multi-Robot Service Prototype](https://arxiv.org/abs/2604.06876)
*Giorgio Audrito,Andrea Basso,Daniele Bortoluzzi,Ferruccio Damiani,Giordano Scarso,Gianluca Torta*

Main category: cs.DC

TL;DR: A prototype multi-robot service system using Aggregate Programming for coordination, validated through simulations and real-world library tests.


<details>
  <summary>Details</summary>
Motivation: Multi-robot systems are increasingly important for applications like healthcare, exploration, and rescue missions, but building them remains challenging due to the complexities of physical robots, environments, and distributed coordination.

Method: Adopts Aggregate Programming (AP) for designing and implementing coordination software in a multi-robot service system prototype, leveraging AP's support for resilient distributed systems with proximity-based communication.

Result: Developed a functional prototype that was validated through both simulations and real-world tests conducted in a University library environment.

Conclusion: Aggregate Programming shows promise as an approach for engineering coordination software in multi-robot systems, successfully enabling the development of a prototype service system validated in both simulated and real-world settings.

Abstract: Multi-robot systems are becoming increasingly relevant within diverse application domains, such as healthcare, exploration, and rescue missions. However, building such systems is still a significant challenge, since it adds the complexities of the physical nature of robots and their environments to those inherent in coordinating any distributed (multi-agent) system. Aggregate Programming (AP) has recently emerged as a promising approach to engineering resilient, distributed systems with proximity-based communication, and is notably supported by practical frameworks.  In this paper we present a prototype of a multi-robot service system, which adopts AP for the design and implementation of its coordination software. The prototype has been validated both with simulations, and with tests in a University library.

</details>


### [16] [On the Decidability of Distributed Tasks with Output Sets under Asynchrony and Any Number of Crashes](https://arxiv.org/abs/2604.06920)
*Timothé Albouy,Antonio Fernández Anta,Chryssis Georgiou,Nicolas Nicolaou,Junlang Wang*

Main category: cs.DC

TL;DR: The paper defines SOS tasks (Set of Output Sets tasks) and proves they are decidable for asynchronous systems with t crashes, showing solvability depends on connectivity of the SOS graph.


<details>
  <summary>Details</summary>
Motivation: To define a new class of distributed tasks that are decidable and to understand solvability conditions for distributed agreement problems without validity constraints.

Method: Define SOS tasks by their set of possible output sets O, construct SOS graph G=(O,⊂) where vertices are output sets and edges connect sets where one includes the other, then analyze connectivity properties.

Result: SOS tasks are decidable: solvable for t=0 always; for t>0, solvable iff SOS graph is connected. This implies k-set agreement is solvable for k>1 under any t≥0, and only consensus (k=1) is unsolvable for t>0 without validity constraints.

Conclusion: The paper establishes a decidable class of distributed tasks with clear solvability conditions, reveals surprising results about k-set agreement without validity, and introduces d-disagreement tasks with harmonic series connections.

Abstract: In this paper, we define a new class of distributed tasks, called SOS tasks (for Set of Output Sets tasks), defined by the set $O$ of distinct output sets of values that can be produced. We then demonstrate that this class of tasks is decidable: there exists an effective procedure that determines whether any SOS task is solvable asynchronously under $t$ crashes. The decision rule is as follows. Every SOS task is solvable when $t=0$. For $t > 0$, an SOS task is solvable if and only if its SOS graph $G=(O,\subset)$ is connected. In this graph, each vertex is an output set in $O$, and two vertices are linked by an edge whenever one output set includes the other. One of the surprising implications of our results is that, without a validity property, $k$-set agreement is solvable under any number of crashes $t \geq 0$ for $k>1$, and unsolvable under $t >0$ crashes only for $k=1$ (consensus). Finally, we study a novel family of tasks called $d$-disagreement, which requires the system to always produce $d$ different output values, and we show that its implementability condition is related to the harmonic series.

</details>


### [17] [NestPipe: Large-Scale Recommendation Training on 1,500+ Accelerators via Nested Pipelining](https://arxiv.org/abs/2604.06956)
*Zhida Jiang,Zhaolong Xing,Huichao Chai,Tianxing Sun,Qiang Peng,Baopeng Yuan,Jiaxing Wang,Hua Du,Zhixin Wu,Xuemiao Li,Yikui Cao,Xinyu Liu,Yongxiang Feng,Zhen Chen,Ke Zhang*

Main category: cs.DC

TL;DR: NestPipe is a decentralized embedding training framework that uses hierarchical sparse parallelism with nested pipelining to overcome data movement bottlenecks in trillion-parameter recommendation models, achieving up to 3.06x speedup while preserving synchronous training semantics.


<details>
  <summary>Details</summary>
Motivation: As recommendation models scale to trillions of parameters and clusters expand to thousands of workers, distributed training bottlenecks shift from computation/memory to data movement (lookup and communication latency). Existing solutions either optimize only one bottleneck or sacrifice training consistency for throughput.

Method: NestPipe uses two hierarchical sparse parallelism techniques: 1) Dual-Buffer Pipelining (DBP) creates a staleness-free five-stage pipeline at inter-batch level to mitigate lookup bottlenecks, and 2) Frozen-Window Pipelining (FWP) overlaps All2All communication with dense computation at intra-batch level through coordinated stream scheduling and key-centric sample clustering.

Result: Experiments on production GPU and NPU clusters with 1,536 workers show NestPipe achieves up to 3.06x speedup and 94.07% scaling efficiency while preserving synchronous training semantics.

Conclusion: NestPipe successfully addresses both lookup and communication bottlenecks in large-scale embedding training through hierarchical sparse parallelism with nested pipelining, maintaining training consistency while significantly improving performance and scaling efficiency.

Abstract: Modern recommendation models have increased to trillions of parameters. As cluster scales expand to O(1k), distributed training bottlenecks shift from computation and memory to data movement, especially lookup and communication latency associated with embeddings. Existing solutions either optimize only one bottleneck or improve throughput by sacrificing training consistency. This paper presents NestPipe, a large-scale decentralized embedding training framework that tackles both bottlenecks while preserving synchronous training semantics. NestPipe exploits two hierarchical sparse parallelism opportunities through nested pipelining. At the inter-batch level, Dual-Buffer Pipelining (DBP) constructs a staleness-free five-stage pipeline through dual-buffer synchronization, mitigating lookup bottlenecks without embedding staleness. At the intra-batch level, we identify the embedding freezing phenomenon, which inspires Frozen-Window Pipelining (FWP) to overlap All2All communication with dense computation via coordinated stream scheduling and key-centric sample clustering. Experiments on production GPU and NPU clusters with 1,536 workers demonstrate that NestPipe achieves up to 3.06x speedup and 94.07% scaling efficiency.

</details>


### [18] [Autopoiesis: A Self-Evolving System Paradigm for LLM Serving Under Runtime Dynamics](https://arxiv.org/abs/2604.07144)
*Youhe Jiang,Ran Yan,You Peng,Wenshuang Li,Taiyi Wang,Fangcheng Fu,Binhang Yuan*

Main category: cs.DC

TL;DR: Autopoiesis is an online self-evolving LLM serving system that continuously evolves serving policies using LLM-driven program synthesis to adapt to runtime dynamics, outperforming state-of-the-art systems by up to 53%.


<details>
  <summary>Details</summary>
Motivation: LLM serving operates in volatile environments with severe runtime dynamics (workload fluctuations, elastic cluster autoscaling). Traditional static serving policies cannot adapt to continuously shifting runtime trade-offs that are workload-specific and evolve over time.

Method: Autopoiesis introduces: 1) LLM-driven program synthesis workflow to evolve serving policies based on real-time observed dynamics, and 2) Continuous online operation that rewrites policy code as runtime trade-offs shift, transforming policy design from offline to ongoing system component.

Result: Evaluation across diverse runtime dynamics shows up to 53% and on average 34% improvements over state-of-the-art LLM serving systems.

Conclusion: Establishes a new paradigm where serving policies are no longer static human-designed artifacts but living code that LLMs continuously evolve throughout deployment to navigate runtime trade-offs beyond human design.

Abstract: Modern Large Language Model (LLM) serving operates in highly volatile environments characterized by severe runtime dynamics, such as workload fluctuations and elastic cluster autoscaling. Traditional serving systems rely on static, human-engineered serving policies (e.g., scheduling algorithms and rescheduling strategies) to manage these dynamics. However, these policies must navigate deeply intertwined runtime trade-offs (e.g., scheduling overhead vs. execution efficiency, rescheduling frequency vs. reconfiguration overhead), whose optimal balance is workload-specific and shifts continuously as runtime conditions evolve, rendering any fixed policy fundamentally unable to adapt. We propose Autopoiesis, a novel online self-evolving system that shifts LLM serving from static policy deployment to continuous online policy evolution. First, Autopoiesis introduces an LLM-driven program synthesis workflow to evolve serving policies with respect to real-time observed dynamics, where the evolved policies reflect the optimal decision in navigating the complex, multi-dimensional trade-off space. Second, Autopoiesis enables this synthesis process to operate continuously during serving, observing real-world system behavior, and rewriting the policy code as runtime trade-offs shift, thereby transforming policy design from a one-time offline endeavor into an ongoing system component, enabling autonomous adaptation to evolving runtime conditions. Together, we establish a new paradigm: Serving policies are no longer static artifacts designed by humans before deployment, but living code that LLMs continuously evolve throughout deployment to navigate runtime trade-offs beyond human design. We evaluate Autopoiesis across diverse runtime dynamics and show up to 53% and on average 34% improvements over state-of-the-art LLM serving systems.

</details>


### [19] [InfiniLoRA: Disaggregated Multi-LoRA Serving for Large Language Models](https://arxiv.org/abs/2604.07173)
*Hongyu Chen,Letian Ruan,Zilin Xu,Yuchen Li,Xinyu Chen,Jingwen Leng,Bingsheng He,Minyi Guo,Shixuan Sun*

Main category: cs.DC

TL;DR: InfiniLoRA is a disaggregated serving system that decouples LoRA execution from base-model inference to address memory and scalability challenges in multi-tenant LLM serving, especially for emerging architectures like MoE.


<details>
  <summary>Details</summary>
Motivation: Existing LoRA serving designs struggle with scalability and tail-latency inflation when dealing with emerging model architectures like MoE, which significantly increase LoRA memory costs in multi-tenant, multi-task serving scenarios.

Method: InfiniLoRA introduces a disaggregated architecture with a shared LoRA Server featuring parallelism-aware execution, SLO-driven provisioning, and critical-path optimizations including GPU-initiated communication and hardware-specialized LoRA kernels.

Result: Experiments show InfiniLoRA achieves 3.05× average increase in serviceable request rate under strict latency SLOs and improves the percentage of LoRA adapters satisfying SLO requirements by 54.0%.

Conclusion: InfiniLoRA effectively addresses scalability and latency challenges in LoRA serving for modern LLM architectures through its disaggregated design and optimization techniques.

Abstract: LoRA enables efficient customization of LLMs and is widely used in multi-tenant and multi-task serving. However, emerging model architectures such as MoE significantly increase LoRA memory cost, making existing coupled LoRA serving designs poorly scalable and prone to tail-latency inflation. We present InfiniLoRA, a disaggregated LoRA serving system that decouples LoRA execution from base-model inference. InfiniLoRA introduces a shared LoRA Server with parallelism-aware execution, SLO-driven provisioning, and critical-path optimizations, including GPU-initiated communication and hardware-specialized LoRA kernels. Experiments show that InfiniLoRA can achieve an average $3.05\times$ increase in serviceable request rate under strict latency SLOs, and improve the percentage of LoRA adapters satisfying the SLO requirement by 54.0\%.

</details>


### [20] [Making Room for AI: Multi-GPU Molecular Dynamics with Deep Potentials in GROMACS](https://arxiv.org/abs/2604.07276)
*Luca Pennati,Andong Hu,Ivy Peng,Lukas Müllender,Stefano Markidis*

Main category: cs.DC

TL;DR: Integration of DeePMD-kit neural network potential into GROMACS enables scalable, GPU-accelerated molecular dynamics with near-quantum accuracy across multi-node systems.


<details>
  <summary>Details</summary>
Motivation: The rise of AI-driven interatomic potentials that offer near-quantum accuracy at molecular dynamics throughput poses a challenge: embedding neural-network inference into multi-GPU simulations while retaining high performance.

Method: Integrated DeePMD-kit into GROMACS with a domain decomposition layer decoupled from main simulation, enabling concurrent inference across all processes using MPI collectives for coordinate broadcasting and force aggregation/redistribution.

Result: Strong-scaling efficiency reached 66% at 16 devices and 40% at 32; weak-scaling efficiency was 80% to 16 devices and 48% (MI250x)/40% (A100) at 32 devices. >90% of wall time spent in DeePMD inference, with MPI collectives contributing <10%.

Conclusion: Production molecular dynamics with near ab initio fidelity is feasible at scale in GROMACS, with principal bottlenecks being irreducible ghost-atom costs from cutoff radius and load imbalance across ranks.

Abstract: GROMACS is a de-facto standard for classical Molecular Dynamics (MD). The rise of AI-driven interatomic potentials that pursue near-quantum accuracy at MD throughput now poses a significant challenge: embedding neural-network inference into multi-GPU simulations retaining high-performance. In this work, we integrate the MLIP framework DeePMD-kit into GROMACS, enabling domain-decomposed, GPU-accelerated inference across multi-node systems. We extend the GROMACS NNPot interface with a DeePMD backend, and we introduce a domain decomposition layer decoupled from the main simulation. The inference is executed concurrently on all processes, with two MPI collectives used each step to broadcast coordinates and to aggregate and redistribute forces. We train an in-house DPA-1 model (1.6 M parameters) on a dataset of solvated protein fragments. We validate the implementation on a small protein system, then we benchmark the GROMACS-DeePMD integration with a 15,668 atom protein on NVIDIA A100 and AMD MI250x GPUs up to 32 devices. Strong-scaling efficiency reaches 66% at 16 devices and 40% at 32; weak-scaling efficiency is 80% to 16 devices and reaches 48% (MI250x) and 40% (A100) at 32 devices. Profiling with the ROCm System profiler shows that >90% of the wall time is spent in DeePMD inference, while MPI collectives contribute <10%, primarily since they act as a global synchronization point. The principal bottlenecks are the irreducible ghost-atom cost set by the cutoff radius, confirmed by a simple throughput model, and load imbalance across ranks. These results demonstrate that production MD with near ab initio fidelity is feasible at scale in GROMACS.

</details>
